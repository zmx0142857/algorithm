# quicker power
pow(base, exp, module):
	ret = 1
	# iterate over each bit of exp
	mask = 1 << |_ log2(exp) _|
	while mask != 0:
		ret = ret**2 % module
		if exp & mask:
			ret = ret * base % module
		mask >> 1
	return ret
